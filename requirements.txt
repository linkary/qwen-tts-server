# Core dependencies
# torch / torchaudio / numpy are intentionally absent here so that this file
# stays portable across CPU and GPU hosts. Do NOT assume a base image provides
# them: the Docker image installs them from the PyTorch CUDA wheel index BEFORE
# this file (see Dockerfile), because otherwise they resolve as transitive deps
# of qwen-tts -> accelerate and pick up whatever CUDA build PyPI defaults to.
# To match the image locally:
#   pip install --index-url https://download.pytorch.org/whl/cu128 \
#       torch==2.8.0 torchaudio==2.8.0

# Audio processing
soundfile>=0.12.1
pydub>=0.25.1
librosa>=0.10.1

# API framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
aiofiles>=23.2.1
sse-starlette>=2.0.0
httpx>=0.26.0

# TTS model
qwen-tts>=0.0.5

# Optional: File validation (may require system dependencies)
python-magic>=0.4.27

# NOTE: flash-attn is OPTIONAL. app/models/manager.py falls back to PyTorch's
# built-in SDPA attention when it is not importable, so skipping it is safe.
# The Docker image installs it from an official prebuilt wheel (see Dockerfile).
# Locally, prefer a prebuilt wheel matching your torch/CUDA/CPython over
# compiling from source (which takes hours and hard-fails if the CUDA major of
# your nvcc differs from the CUDA major your torch was built against):
#   https://github.com/Dao-AILab/flash-attention/releases
# Or skip it and set USE_FLASH_ATTENTION=false in .env

# Development/Testing dependencies are in requirements-dev.txt
# Install with: pip install -r requirements-dev.txt

