# Environment variables for Vite
# Uncomment and set this to your backend's base URL if you want the frontend to call the backend API directly instead of relying on the Vite proxy.
VITE_API_BASE_URL=https://localhost:8000
